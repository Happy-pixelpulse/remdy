import '../screen/doctor_list.dart';

class FilterListManager {
  final FilterEnum filterEnum;
  final String filterName;

  const FilterListManager({required this.filterEnum, required this.filterName});
}
